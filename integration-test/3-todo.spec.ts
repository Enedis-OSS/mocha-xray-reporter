const assert= require('assert');

it('Should be at position 3 with position 1 and 2 as TODO @TICKET-3 @STEP-3', () => {
    assert(true);
});

it('Should be at position 6 with position 4 and 5 as TODO @TICKET-3 @STEP-6', () => {
    assert(true);
});

