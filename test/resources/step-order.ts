const assert= require('assert');

it('Should be at position 12 @TICKET-1 @STEP-12', () => {
    assert(true);
});

it('Should be at position 11 @TICKET-1 @STEP-11', () => {
    assert(true);
});

it('Should be at position 10 @TICKET-1 @STEP-10', () => {
    assert(true);
});

it('Should be at position 9 @TICKET-1 @STEP-9', () => {
    assert(true);
});

it('Should be at position 8 @TICKET-1 @STEP-8', () => {
    assert(true);
});

it('Should be at position 7 @TICKET-1 @STEP-7', () => {
    assert(true);
});

it('Should be at position 6 @TICKET-1 @STEP-6', () => {
    assert(true);
});

it('Should be at position 5 @TICKET-1 @STEP-5', () => {
    assert(true);
});

it('Should be at position 4 @TICKET-1 @STEP-4', () => {
    assert(true);
});

it('Should be at position 3 @TICKET-1 @STEP-3', () => {
    assert(true);
});

it('Should be at position 2 @TICKET-1 @STEP-2', () => {
    assert(true);
});

it('Should be at position 1 @TICKET-1 @STEP-1', () => {
    assert(true);
});
