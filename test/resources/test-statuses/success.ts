it('Success and ticket will be PASS @TICKET-1 @STEP-1', () => {
    const assert= require('assert');
    assert(true);
});

it('Success and ticket will be PASS @TICKET-1 @STEP-2', () => {
    const assert= require('assert');
    assert(true);
});