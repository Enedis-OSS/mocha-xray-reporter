it('PASS and ticket will be TODO @TICKET-1 @STEP-1', () => {
    const assert= require('assert');
    assert(true);
});

it('PENDING because of @MANUAL and ticket will be TODO @TICKET-1 @STEP-3 ', () => {
    const assert= require('assert');
    assert(true);
});

it.skip('RENONCEMENT and ticket will be TODO @TICKET-1 @STEP-4 ', () => {
    const assert= require('assert');
    assert(true);
});