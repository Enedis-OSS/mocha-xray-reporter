it('Success and ticket will be PENDING @TICKET-1 @STEP-1', () => {
    const assert= require('assert');
    assert(true);
});

it('@MANUAL and ticket will be PENDING @TICKET-1 @STEP-2', () => {
    const assert= require('assert');
    assert(true);
});