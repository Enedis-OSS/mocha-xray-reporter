it('PASS and ticket will be RENONCEMENT @TICKET-1 @STEP-1', () => {
    const assert= require('assert');
    assert(true);
});

it('PENDING and ticket will be RENONCEMENT @TICKET-1 @STEP-2', () => {
    const assert= require('assert');
    assert(true);
});

it.skip('RENONCEMENT and ticket will be RENONCEMENT @TICKET-1 @STEP-3 ', () => {
    const assert= require('assert');
    assert(true);
});